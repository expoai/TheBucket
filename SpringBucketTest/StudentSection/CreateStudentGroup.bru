meta {
  name: CreateStudentGroup
  type: http
  seq: 1
}

post {
  url: {{connectionPath}}/admin/create-team?name=Testing
  body: none
  auth: bearer
}

params:query {
  name: Testing
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  var jsonData = res.getBody();
  bru.setEnvVar("studentToken", jsonData.apiKey);
}
