meta {
  name: Get By ID
  type: http
  seq: 5
}

get {
  url: {{connectionPath}}/generation-request/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

docs {
  ## Get Single Generation Request
  
  Replace the ID in the URL (currently `/1`) with the desired generation request ID.
  
  Returns a single generation request object or 404 if not found.
}
