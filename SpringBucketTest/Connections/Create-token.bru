meta {
  name: Create-token
  type: http
  seq: 2
}

post {
  url: {{connectionPath}}/api-token
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  var jsonData = res.getBody();
  bru.setEnvVar("apiToken", jsonData.token);
}
