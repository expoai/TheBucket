meta {
  name: Setup Admin
  type: http
  seq: 1
}

post {
  url: {{connectionPath}}/setup/create-admin
  body: json
  auth: inherit
}

body:json {
  {
    "username" : "{{adminUser}}",
    "password" : "{{adminPassword}}"
  }
}
